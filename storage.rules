rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite que um usuário gerencie sua própria foto de perfil
    match /profile-pictures/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
